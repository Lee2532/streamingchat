service:
  enabled: true
  type: LoadBalancer
  port: 3000

adminPassword: admin

datasources:
  datasources.yaml:
    apiVersion: 1
    datasources:
    - name: Prometheus
      type: prometheus
      url: http://host.docker.internal:80
      access: proxy
      isDefault: true
